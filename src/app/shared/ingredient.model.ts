export class Ingredient {

    //no need of declaring variable & assigning params in constructor, just use public before taking args in constructor

    constructor(public name: string, public amount: number) {

    }
}